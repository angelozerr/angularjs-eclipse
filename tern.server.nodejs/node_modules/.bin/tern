#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tern/bin/tern" "$@"
  ret=$?
else 
  node  "$basedir/../tern/bin/tern" "$@"
  ret=$?
fi
exit $ret
